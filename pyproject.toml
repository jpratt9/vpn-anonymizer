[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "vpn_anonymizer"
version = "0.1.0"
description = "Cycle through Mullvad WireGuard relays and reputation-check exit IPs."
requires-python = ">=3.10"
dependencies = []

[project.scripts]
vpn-anonymizer = "vpn_anonymizer:main"

[tool.setuptools]
py-modules = ["vpn_anonymizer"]
